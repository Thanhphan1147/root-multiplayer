# Long random secret that signs every seat token. Generate with:
#   openssl rand -hex 32
RMN_SECRET=change-me-in-production

# Cloudflare Tunnel token (named tunnel). Get it from the Zero Trust dashboard:
# Networking -> Tunnels -> your tunnel -> the install command contains the token.
CF_TUNNEL_TOKEN=
